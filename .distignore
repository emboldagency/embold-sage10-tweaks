# A set of files you probably don't want in your WordPress.org distribution
.deploy
.distignore
.DS_Store
.editorconfig
.env
.git
.github
.gitignore
.gitmodules
.gitlab-ci.yml
.vscode
*.sql
*.tar.gz
*.zip
bin
composer.json
composer.lock
dist
docker
docker-compose.yml
Gruntfile.js
node_modules
package.json
phpcs.xml
phpcs.xml.dist
phpcs.ruleset.xml
phpstan.neon.dist
phpunit.xml
phpunit.xml.dist
scripts
README.md
tests
Thumbs.db
wp-cli.local.yml

# Explicitly include vendor directory (required for plugin functionality).
# Anchored to root so it only re-includes the top-level vendor, not any nested
# vendor dirs (e.g. inside an otherwise-excluded dist/extracted).
!/vendor
!/vendor/**
